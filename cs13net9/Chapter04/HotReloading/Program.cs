/* Visual Studio: run the app, change the messafe, click Hot Reload.
 * VS Code: run the app using dotnet watch, change the mesasge. */

while (true)
{
  Console.WriteLine("Goodbye, Hot Reload!");
  await Task.Delay(2000);
}
